{
  "name": "core",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ den13t/core",
    "create-local": "graph create --node http://localhost:8020/ den13t/core",
    "remove-local": "graph remove --node http://localhost:8020/ den13t/core",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 den13t/core"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
